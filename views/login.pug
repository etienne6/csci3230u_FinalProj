extends layout

block content
    form(method="post",
        action="/processLogin"
        class="form")
        div
            p UserName
            input(type="text"
                  id="username"
                  name="username")
        div
            p Password
            input(type="password"
                  id="password"
                  name="password")
        div
            button Log In